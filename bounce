var r=16;
function setup() 
{
  createCanvas(400, 400);
}
function draw() {
  background(220);
  for(var i=35;i>0;i--)
  {
    if(i%2==0)
    { 
      fill(220); 
      ellipse(200,200,r*i)；
    }
    else
    {
      fill(50);
      ellipse(200,200,r*i);
    }
  }
}
